# MongoDB backend alignment

Set `VITE_API_BASE_URL` to the deployed MongoDB backend URL ending in `/api`.

Core connected modules: authentication, dashboards, categories, global products, outlets, outlet inventory, outlet credentials, orders and lifecycle actions, invoices, online transactions, dynamic Razorpay/Google Maps settings, delivery/rider rates, feature toggles, daily closing and stock ledger.

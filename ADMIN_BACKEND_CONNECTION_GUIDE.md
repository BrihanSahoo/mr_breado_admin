# Admin Backend Connection

Set the production API URL before building:

```env
VITE_API_BASE_URL=https://YOUR-RENDER-SERVICE.onrender.com/api
```

The project is aligned with the MongoDB Atlas backend package supplied together with this Admin web build.

Never include a trailing `/api/api`; the variable must end exactly once with `/api`.
